"""AutoML Agent System: a LangGraph-based plan/train/evaluate/replan loop.

Docstrings and comments in this package cite paths it does not ship — ``docs/rationale.md``,
``docs/RESULTS.md``, ``bench/paired.py``, ``tests/test_privacy.py`` and the like. Those are files in
the development repository, and each one opens at the commit these citations were written against:

    https://github.com/aooty/AutoML-Agent/tree/2ac7613ff99db04f73f1e8fec8c86f999ebe76bf

A commit rather than a branch, so a number a citation quotes stays the number at that path.

``docs/rationale.md`` is the one to know about: the arguments behind the guards in this package were
moved out of the docstrings into that file, so a comment here often states the rule and cites the
reasoning rather than carrying it. Nine paths are cited in total and all nine open at that commit.
"""

__version__ = "0.1.0"

"""AutoML Agent System: a LangGraph-based plan/train/evaluate/replan loop.

Docstrings and comments in this package cite paths it does not ship — ``docs/RESULTS.md``,
``bench/paired.py``, ``tests/test_privacy.py`` and the like. Those are files in the development
repository, and each one opens at the commit these citations were written against:

    https://github.com/aooty/automl-agent/tree/fe2f70cf595ebb2888456346ab422e8f829141ea

A commit rather than a branch, so a number a citation quotes stays the number at that path.

One cited path is in neither repository: ``local/skew_ceiling_probe.py``. ``local/`` is ignored by
git in both, because the data under it is held under a use agreement that forbids redistribution.
The constructed counterexample it names is described in full where it is cited.
"""

__version__ = "0.1.0"
